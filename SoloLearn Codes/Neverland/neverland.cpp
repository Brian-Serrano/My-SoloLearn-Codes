#include <iostream>
using namespace std;

int main() {
    int a, p;
    cin >> a >> p;

    cout << "My twin is " << a + p << " years old and they are " << p << " years older than me";

    return 0;
}